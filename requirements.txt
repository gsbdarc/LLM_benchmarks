ipykernel
pandas
python-dotenv
openai
pdf2image
pydantic
tabulate
instructor
ollama-instructor
matplotlib

# Agentic metric-eval (mcp/eval) + observability/analysis layer
mcp==1.28.1       # PINNED: 2.x removed mcp.client.streamable_http.streamablehttp_client (breaks runtime/agent.py)
pymongo
weave
tenacity          # retry/backoff on NIM/MCP connection drops
requests          # scrape vLLM /v1/metrics
psutil            # client RAM sampling
pyarrow           # Parquet run-summary sink
duckdb            # OLAP queries over the Parquet runs
starlette         # analysis/serve_dashboard.py ASGI app (ships with mcp, pinned here for clarity)
uvicorn           # analysis/serve_dashboard.py server

# Tests
pytest
pytest-asyncio