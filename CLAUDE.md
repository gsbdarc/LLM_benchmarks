@AGENTS.md

# Claude Code

- Load the repository's canonical instructions above; do not maintain a second copy here.
- Use the `agentic-eval` skill for detailed work on `agent_eval/` and `analysis/`.
