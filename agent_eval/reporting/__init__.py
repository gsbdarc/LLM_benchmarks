"""reporting — measuring and recording a run: tracing/derivations/metrics scrape
(`observability`), post-run scoring (`scorers`), integrity checks (`integrity`), and the
one-row-per-run Parquet sink (`sink`)."""
