"""registry — the eval-work registry and its data prep: pure build/dedupe/sample logic
(`mapping`), the Mongo-facing CLI that writes `inputs/eval_mapping.csv`
(`create_eval_mapping`), and the gold-label generator (`generate_gold_metrics`)."""
