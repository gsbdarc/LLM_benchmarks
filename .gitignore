__pycache__/
logs/
snapshots/
venv/
.venv/
inputs/data/
*.swp
.ipynb_checkpoints/
outputs/

# Secrets — never commit
.env

# Python build/test artifacts
*.pyc
.pytest_cache/

# Stray backups + job logs
.gitignore.save*
*.out
*.log

# Generated eval-run registry (produced by agent_eval.registry.create_eval_mapping)
inputs/eval_mapping.csv
inputs/eval_mapping_sample.csv

# Generated (data-laden) dashboard from the self-rescheduling refresh — the tracked
# dashboard_template.html is the data-free shell (tokenviz hygiene).
images/agent_dashboard_live.html
